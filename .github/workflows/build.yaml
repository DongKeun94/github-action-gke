name: Deploy test app

on:
  push:
    branches:
    - "master"

jobs:
  deploy:
    permissions:
      contents: 'read'
      id-token: 'write' 
    
    runs-on: ubuntu-latest
   
    steps:
    - name: code checkout
      uses: actions/checkout@v4
    
    - id: 'auth'
      uses: 'google-github-actions/auth@v2'
      with:
        workload_identity_provider: ${{ secrets.GOOGLE_WORKLOAD_IDENTITY_PROVIDER}}
        service_account: ${{ secrets.GOOGLE_SA }}

    - name: install the gcloud cli
      uses: google-github-actions/setup-gcloud@v2
    
    - name: build and push the docker image
      env:
        GOOGLE_PROJECT: ${{ secrets.GOOGLE_PROJECT }}
      run: |
        IMAGE_TAG=$(date +'%Y%m%d%H%M%S')      
        gcloud auth configure-docker asia-northeast3-docker.pkg.dev
        docker build -t asia-northeast3-docker.pkg.dev/$GOOGLE_PROJECT/test-repo/images/test:$IMAGE_TAG .
        docker push asia-northeast3-docker.pkg.dev/$GOOGLE_PROJECT/test-repo/images/test:$IMAGE_TAG

    - name: Set up Helm
      uses: azure/setup-helm@v1
      with:
        version: 'latest'

    - name: Package Helm chart
      run: |
        helm package go-test/
        helm init --stable-repo-url=https://charts.helm.sh/stable
        helm push go-test-0.1.0.tgz oci://asia-northeast3-docker.pkg.dev/$GOOGLE_PROJECT/test-repo/charts/test:$IMAGE_TAG
